apiVersion: v1
kind: Service
metadata:
  name: {{ include "syft-generator-chart.fullname" . }}
  labels:
    {{- include "syft-generator-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "syft-generator-chart.selectorLabels" . | nindent 4 }}
